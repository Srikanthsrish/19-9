const div = document.createElement('div');
const blockquote = document.createElement('blockquote');
blockquote.innerText = 'This is a quote.';
div.appendChild(blockquote);
document.body.appendChild(div);
